Answering the question no. 1

var: var was used in previous days. It has some confusing behavior. Now-a-days var is not used by programmer.

let: let is used when the value can be changed later.

const: const is used whe the value can not be changed later.


.....................................................................................................................

Answering the question no. 2

... is used when need to spread values from array or object. It also used to copy values from array and object.


.....................................................................................................................

Answering the question no. 3

map(): This function is used when we need to create a new array by changing its values. But it will not change the original array.

filter(): this function is used when we want keep only specific items and remove unnecessary items from array. Basically here a condition will run to keep our desire items.

forEach(): This is a loop. It is like another loops to run code for each items.


.....................................................................................................................

Answering the question no. 4

arrow function is also same as normal function. It is used in ES6. This function is basically used to short the function. arrow function is used in modern JavaScript.


.....................................................................................................................

Answering the question no. 5

Template literals is used to insert variable into strings. This is used `` or backticks for multiline strings. So we can use any expression using ${expression}. This is very powerful and helpful for modern programming.